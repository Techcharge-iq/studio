# Example env vars used by the app on Vercel
# Add these in your Vercel Project > Settings > Environment Variables

# Node runtime used by Vercel — ensure project uses Node 18+ in Vercel settings
NODE_VERSION=18

# Example database connection (set in Vercel as a secure variable)
DATABASE_URL="postgresql://user:password@hostname:5432/hrmspark"

# Next public variables
NEXT_PUBLIC_APP_NAME=HRMSpark

# Example secret used by server code or NextAuth (if you add auth)
NEXTAUTH_SECRET="replace_with_strong_secret"
